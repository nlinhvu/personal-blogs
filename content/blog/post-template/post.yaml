# Shared across both languages. The directory name is the slug and the
# pairing key — it is deliberately not a field in here.

# Keep this line while the post is being written and translated; delete it at
# step 6 of the publishing runbook. It is also what keeps THIS file — the
# template itself — off the public site, which is where it belongs.
draft: true

pubDate: 2026-08-24T09:00:00+07:00
tags: [spring-boot, jvm-internals]
source: en
